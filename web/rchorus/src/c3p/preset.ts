import { Preset } from "../preset";

export const preset: Preset = {
  rate: 0.35,
  depth: 100,
  delay_scale: 1,
  mix: 100,
  highpass_cutoff: "Low",
  routing: "Synth",
};

export default preset;
