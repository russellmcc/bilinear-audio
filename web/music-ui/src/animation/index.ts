import useAnimation, { CustomAnimation } from "./useAnimation";
import { fade, easeIn } from "./util";

export type { CustomAnimation };
export { useAnimation, fade, easeIn };
export {
  useSmoothedValue,
  type Options as UseSmoothedValueOptions,
} from "./useSmoothedValue";
